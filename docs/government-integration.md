# Government integration disclosure

Current prototype integration is **simulated**. `MockCentralGovernmentAdapter` and `MockTamilNaduAdapter` contain local synthetic records. They never call, scrape, reverse-engineer, or authenticate against government systems.

Future production work requires an approved official integration, farmer consent, scoped authorization, minimum data collection, secure audit logs, and a state adapter per authorized service.
